<li class="list-none">
    <%= cart_item.product.name %>
    <%= cart_item.product.price %>
    <p>数量：<%= cart_item.qty %></p>
    <%= button_to "削除", cart_item_path(product_id: cart_item.product.id), method: :delete, data: { confirm: "商品を削除しますか？" } %>
</li>

